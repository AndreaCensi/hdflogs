language: python
reset_minion: true

python:
  - 2.7
  # - 3.2
  # - 3.3
  # - 3.4
  # - pypy
  
install:
  - sudo apt-get install python-tables python-numexpr
  - pip install -r requirements.txt
 
script:
  - nosetests hdflog
  - nosetests rawlogs_hdflog
 