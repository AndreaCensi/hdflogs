language: python
reset_minion: true
build_environment: Ubuntu 12.04

python:
  - 2.7
  # - 3.2
  # - 3.3
  # - 3.4
  # - pypy
  
install:
  # install right dependencies in the system
  - sudo apt-get install python-tables libhdf5-lam-dev
  # now install in the virtualenv
  - pip install numexpr
  - pip install cython
  - pip install tables
  - "python -c 'import tables'"
  - pip install -r requirements.txt
 
script:
  - nosetests hdflog
  - nosetests rawlogs_hdflog
 
 